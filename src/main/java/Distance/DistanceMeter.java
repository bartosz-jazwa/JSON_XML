package Distance;

public class DistanceMeter {

    /*public static void main(String[] args) throws IOException {
        Path path = Paths.get("punkty.txt");
        List<String > plik = Files.lines(path).collect(Collectors.toList());

        plik.stream()
                .map(s -> s.split(" "))

    }*/

}
